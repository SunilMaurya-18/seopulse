spring:
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.seopulse: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN

seopulse:
  crawler:
    max-pages: 1000
    max-depth: 10
  worker:
    outbox-batch-size: 100

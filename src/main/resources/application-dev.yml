spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.seopulse: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
